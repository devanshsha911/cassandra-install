cluster_name: "{{ cluster_name }}"

listen_address: {{ listen_address }}

rpc_address: {{ rpc_address }}

seed_provider:
  - class_name: org.apache.cassandra.locator.SimpleSeedProvider
    parameters:
      - seeds: "{{ seed_nodes | join(',') }}"
